/**
 * Shared LoadingState — re-export with documented variants.
 * Variants: 'spinner' (actions, full-block), 'skeleton' (content placeholders), 'inline' (inline content).
 */
export { LoadingState } from '../ui/LoadingState';
