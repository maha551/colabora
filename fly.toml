# fly.toml app configuration file for Colabora
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.

app = 'colabora-fresh'
primary_region = 'fra'

[build]
  dockerfile = "Dockerfile"
  # Increase build timeout for slower builds
  build_timeout = "15m"

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

# Environment variables for production
[env]
  NODE_ENV = "production"
  PORT = "3000"
  FRONTEND_URL = "https://colabora-fresh.fly.dev"
  ALLOWED_ORIGINS = "https://colabora-fresh.fly.dev,https://*.colabora-fresh.fly.dev"
  LOG_LEVEL = "info"
  JWT_EXPIRES_IN = "8h"
  RATE_LIMIT_WINDOW_MS = "900000"
  RATE_LIMIT_MAX_REQUESTS = "500"

# Required secrets (will be set via fly secrets set)
# SESSION_SECRET - Required for session security
# JWT_SECRET - Required for JWT token signing
# DATABASE_URL - Set to /data/colabora.db (volume mount)

# Mount persistent volume for SQLite database (3GB)
[mounts]
  source = "colabora_data"
  destination = "/data"

[[vm]]
  memory = '2gb'
  cpu_kind = 'shared'
  cpus = 1

# Health checks
[checks]
  [checks.health]
    port = 3000
    type = "http"
    interval = "30s"
    timeout = "10s"
    grace_period = "60s"
    method = "GET"
    path = "/api/health/ready"
