# Copy to .env.data on the data server (do not commit .env.data)

POSTGRES_DB=colabora
POSTGRES_USER=colabora
POSTGRES_PASSWORD=change-me-long-random

REDIS_PASSWORD=change-me-long-random

# In production, bind to the server's private network IP so only app servers can connect:
# POSTGRES_BIND=10.0.0.20
# REDIS_BIND=10.0.0.20
